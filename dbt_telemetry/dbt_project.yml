name: 'dbt_telemetry'
version: '1.0.0'
config-version: 2

profile: 'dbt_telemetry'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_telemetry:
    staging:
      +materialized: view
    marts:
      +materialized: table
